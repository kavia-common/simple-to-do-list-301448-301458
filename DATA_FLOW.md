# Todo App - Data Flow Documentation

This document visualizes how data flows through the todo application from user interaction to database persistence.

---

## 🔄 Complete CRUD Flow

### 1️⃣ CREATE TODO - Add New Task

```
┌─────────────┐
│    USER     │
│ Enters text │
│ Clicks "Add"│
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  FRONTEND (React - Port 3000)                │
│                                              │
│  Component: TodoInput.js                     │
│  ├─ State: title, description               │
│  ├─ Event: handleSubmit()                   │
│  └─ Calls: createTodo({title, description}) │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP POST /todos
       │ Body: {"title": "...", "description": "...", "completed": false}
       │ Headers: Content-Type: application/json
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND (FastAPI - Port 3001)               │
│                                              │
│  Route: POST /todos                          │
│  ├─ Validates: TodoCreate schema (Pydantic) │
│  ├─ Generates: ID, timestamps               │
│  └─ Calls: db.execute(INSERT INTO todos...) │
└──────┬───────────────────────────────────────┘
       │
       │ SQL: INSERT INTO todos (title, description, completed, 
       │      created_at, updated_at) VALUES (?, ?, ?, ?, ?)
       │
       ▼
┌──────────────────────────────────────────────┐
│  DATABASE (SQLite)                           │
│                                              │
│  File: myapp.db                              │
│  └─ Table: todos                             │
│     └─ Row: [id=1, title="...", ...]        │
└──────┬───────────────────────────────────────┘
       │
       │ Returns: Row ID = 1
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND (FastAPI)                           │
│                                              │
│  ├─ Fetches: Newly created todo             │
│  └─ Returns: HTTP 201 + TodoResponse JSON   │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP 201 Created
       │ Body: {"id": 1, "title": "...", "description": "...", 
       │        "completed": false, "created_at": "...", "updated_at": "..."}
       │
       ▼
┌──────────────────────────────────────────────┐
│  FRONTEND (React)                            │
│                                              │
│  Component: App.js                           │
│  ├─ Receives: New todo object               │
│  ├─ Updates: setTodos([...todos, newTodo])  │
│  └─ Re-renders: TodoList                     │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌─────────────┐
│    USER     │
│ Sees new    │
│ todo in list│
└─────────────┘
```

---

### 2️⃣ READ TODOS - Load Task List

```
┌─────────────┐
│    USER     │
│ Opens page  │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  FRONTEND (React)                            │
│                                              │
│  Hook: useEffect(() => loadTodos(), [])      │
│  └─ Calls: getTodos()                        │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP GET /todos
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND (FastAPI)                           │
│                                              │
│  Route: GET /todos                           │
│  └─ Calls: db.execute(SELECT * FROM todos)   │
└──────┬───────────────────────────────────────┘
       │
       │ SQL: SELECT id, title, description, completed, 
       │      created_at, updated_at FROM todos
       │
       ▼
┌──────────────────────────────────────────────┐
│  DATABASE (SQLite)                           │
│                                              │
│  └─ Returns: All rows from todos table       │
└──────┬───────────────────────────────────────┘
       │
       │ Returns: List of todo rows
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND (FastAPI)                           │
│                                              │
│  ├─ Maps: Rows to TodoResponse objects      │
│  └─ Returns: HTTP 200 + TodoListResponse     │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP 200 OK
       │ Body: {"todos": [...], "total": 5}
       │
       ▼
┌──────────────────────────────────────────────┐
│  FRONTEND (React)                            │
│                                              │
│  ├─ Receives: Array of todos                │
│  ├─ Updates: setTodos(data)                 │
│  └─ Renders: TodoItem for each todo         │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌─────────────┐
│    USER     │
│ Sees todo   │
│ list        │
└─────────────┘
```

---

### 3️⃣ UPDATE TODO - Edit Task

```
┌─────────────┐
│    USER     │
│ Clicks Edit │
│ Changes text│
│ Clicks Save │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  FRONTEND (React)                            │
│                                              │
│  Component: TodoItem.js                      │
│  ├─ State: isEditing=true, editTitle="..."  │
│  ├─ Event: handleSave()                     │
│  └─ Calls: updateTodo(id, {title, desc})    │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP PUT /todos/{id}
       │ Body: {"title": "...", "description": "...", "completed": ...}
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND (FastAPI)                           │
│                                              │
│  Route: PUT /todos/{id}                      │
│  ├─ Validates: TodoUpdate schema            │
│  ├─ Updates: updated_at timestamp           │
│  └─ Calls: db.execute(UPDATE todos...)       │
└──────┬───────────────────────────────────────┘
       │
       │ SQL: UPDATE todos SET title=?, description=?, 
       │      updated_at=? WHERE id=?
       │
       ▼
┌──────────────────────────────────────────────┐
│  DATABASE (SQLite)                           │
│                                              │
│  └─ Updates: Row with matching id           │
└──────┬───────────────────────────────────────┘
       │
       │ Returns: Number of rows affected (1)
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND (FastAPI)                           │
│                                              │
│  ├─ Fetches: Updated todo                   │
│  └─ Returns: HTTP 200 + TodoResponse        │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP 200 OK
       │ Body: {"id": 1, "title": "UPDATED", ...}
       │
       ▼
┌──────────────────────────────────────────────┐
│  FRONTEND (React)                            │
│                                              │
│  ├─ Receives: Updated todo object           │
│  ├─ Updates: setTodos(todos.map(...))       │
│  └─ Re-renders: TodoItem with new data      │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌─────────────┐
│    USER     │
│ Sees updated│
│ todo        │
└─────────────┘
```

---

### 4️⃣ COMPLETE TODO - Toggle Checkbox

```
┌─────────────┐
│    USER     │
│ Clicks      │
│ checkbox    │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  FRONTEND (React)                            │
│                                              │
│  Component: TodoItem.js                      │
│  ├─ Event: onChange(checkbox)               │
│  └─ Calls: toggleTodoComplete(id, !checked) │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP PATCH /todos/{id}/complete
       │ Body: {"completed": true}
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND (FastAPI)                           │
│                                              │
│  Route: PATCH /todos/{id}/complete           │
│  ├─ Validates: TodoComplete schema          │
│  └─ Calls: db.execute(UPDATE todos...)       │
└──────┬───────────────────────────────────────┘
       │
       │ SQL: UPDATE todos SET completed=?, 
       │      updated_at=? WHERE id=?
       │
       ▼
┌──────────────────────────────────────────────┐
│  DATABASE (SQLite)                           │
│                                              │
│  └─ Updates: completed field                │
└──────┬───────────────────────────────────────┘
       │
       │ Returns: Number of rows affected (1)
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND (FastAPI)                           │
│                                              │
│  ├─ Fetches: Updated todo                   │
│  └─ Returns: HTTP 200 + TodoResponse        │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP 200 OK
       │ Body: {"id": 1, "completed": true, ...}
       │
       ▼
┌──────────────────────────────────────────────┐
│  FRONTEND (React)                            │
│                                              │
│  ├─ Receives: Updated todo                  │
│  ├─ Updates: State                          │
│  └─ Applies: CSS class "completed"          │
│     (strikethrough, opacity)                │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌─────────────┐
│    USER     │
│ Sees        │
│ strikethrough│
└─────────────┘
```

---

### 5️⃣ DELETE TODO - Remove Task

```
┌─────────────┐
│    USER     │
│ Clicks      │
│ Delete      │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  FRONTEND (React)                            │
│                                              │
│  Component: TodoItem.js                      │
│  ├─ Event: onClick(delete button)           │
│  └─ Calls: deleteTodo(id)                   │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP DELETE /todos/{id}
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND (FastAPI)                           │
│                                              │
│  Route: DELETE /todos/{id}                   │
│  └─ Calls: db.execute(DELETE FROM todos...)  │
└──────┬───────────────────────────────────────┘
       │
       │ SQL: DELETE FROM todos WHERE id=?
       │
       ▼
┌──────────────────────────────────────────────┐
│  DATABASE (SQLite)                           │
│                                              │
│  └─ Deletes: Row with matching id           │
└──────┬───────────────────────────────────────┘
       │
       │ Returns: Number of rows affected (1)
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND (FastAPI)                           │
│                                              │
│  └─ Returns: HTTP 204 No Content            │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP 204 No Content
       │ (Empty body)
       │
       ▼
┌──────────────────────────────────────────────┐
│  FRONTEND (React)                            │
│                                              │
│  ├─ Receives: 204 status                    │
│  ├─ Updates: setTodos(todos.filter(...))    │
│  └─ Re-renders: TodoList without deleted    │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌─────────────┐
│    USER     │
│ Todo        │
│ disappears  │
└─────────────┘
```

---

## 🔐 CORS Flow

```
┌──────────────────────────────────────────────┐
│  BROWSER (http://localhost:3000)             │
│                                              │
│  About to make: POST /todos                  │
│  Cross-origin: localhost:3000 → :3001       │
└──────┬───────────────────────────────────────┘
       │
       │ Preflight: OPTIONS /todos
       │ Headers:
       │   Origin: http://localhost:3000
       │   Access-Control-Request-Method: POST
       │   Access-Control-Request-Headers: Content-Type
       │
       ▼
┌──────────────────────────────────────────────┐
│  BACKEND CORS Middleware                     │
│                                              │
│  Checks: Is origin allowed?                  │
│  ├─ ALLOWED_ORIGINS includes localhost:3000 │
│  ├─ Method POST is allowed                  │
│  └─ Header Content-Type is allowed          │
└──────┬───────────────────────────────────────┘
       │
       │ HTTP 200 OK
       │ Headers:
       │   Access-Control-Allow-Origin: http://localhost:3000
       │   Access-Control-Allow-Methods: POST, GET, ...
       │   Access-Control-Allow-Headers: Content-Type, ...
       │   Access-Control-Allow-Credentials: true
       │
       ▼
┌──────────────────────────────────────────────┐
│  BROWSER                                     │
│                                              │
│  ✅ Preflight passed                         │
│  └─ Proceeds with actual POST request       │
└──────────────────────────────────────────────┘
```

---

## 📊 State Management

### Frontend State Flow

```
App.js (Parent)
├─ State: todos = []
├─ State: loading = true
├─ State: error = null
│
├─ useEffect → loadTodos()
│   └─ setTodos([...])
│
├─ Renders: TodoInput
│   └─ onAdd → handleAddTodo()
│       └─ setTodos([...todos, newTodo])
│
└─ Renders: TodoItem (for each todo)
    ├─ onUpdate → handleUpdateTodo()
    │   └─ setTodos(todos.map(...))
    ├─ onDelete → handleDeleteTodo()
    │   └─ setTodos(todos.filter(...))
    └─ onToggleComplete → handleToggleComplete()
        └─ setTodos(todos.map(...))
```

---

## 🗄️ Database Schema

```sql
CREATE TABLE todos (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT,
    completed INTEGER DEFAULT 0,  -- SQLite uses 0/1 for boolean
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);

-- Example Row:
-- | id | title          | description     | completed | created_at          | updated_at          |
-- |----|----------------|-----------------|-----------|---------------------|---------------------|
-- | 1  | Buy groceries  | Milk, eggs      | 0         | 2025-12-23 10:00:00 | 2025-12-23 10:00:00 |
```

---

## 🌐 Network Layer

### Request Headers (Frontend → Backend)
```http
POST /todos HTTP/1.1
Host: localhost:3001
Content-Type: application/json
Origin: http://localhost:3000
Content-Length: 75

{"title":"Buy groceries","description":"Milk, eggs","completed":false}
```

### Response Headers (Backend → Frontend)
```http
HTTP/1.1 201 Created
Content-Type: application/json
Access-Control-Allow-Origin: http://localhost:3000
Access-Control-Allow-Credentials: true
Content-Length: 156

{"id":1,"title":"Buy groceries","description":"Milk, eggs","completed":false,"created_at":"2025-12-23 10:00:00","updated_at":"2025-12-23 10:00:00"}
```

---

## ⚡ Performance Optimization

### Frontend Optimizations
- React.StrictMode for development checks
- Component state isolation (TodoItem manages own edit state)
- Conditional rendering (loading, empty states)
- CSS transitions for smooth UX

### Backend Optimizations
- SQLite connection pooling (implicit)
- Pydantic validation (fast C bindings)
- Single database file (no network overhead)
- Indexed primary key for fast lookups

---

**Last Updated**: 2025-12-23  
**Status**: All data flows verified and operational
